/*$(document).ready(function() {

$(window).on("resize", function(){
 alert($("ds-header").height());
})

   
    $("ds-view").css({
      "position" : "absolute",
      "left" : $(".mainMenu").width(),
      "top" : $("ds-header").height(),
      "width" : $(window).innerWidth() - $(".mainMenu").width(),
      "height" : $(window).innerHeight() - $(".ds-header").height(),
      "overflow": "auto"
    })
})*/